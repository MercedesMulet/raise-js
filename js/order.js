/* const cart = new Cart();
const cartTwo = document.getElementById("cart-icon");
const cartList = document.querySelector('#cartList tbody');

addEvents();

function addEvents(){
    ecomSection.addEventListener('click', (e)=>{cart.buyProduct(e.target.parentNode.children[0].innerHTML)});
} */

/* const cart = new Cart();
const cartTwo = document.getElementById("cart-icon");
const cartList = document.querySelector('#cartList tbody');

addEvents();

function addEvents(){
    btnElement.addEventListener("click", (e)=>{console.log(e.target);})
}
 */